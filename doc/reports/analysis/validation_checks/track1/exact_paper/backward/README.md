# Track 1 Exact Paper Backward Placeholder

This folder is reserved for future `backward` exact-paper validation reports.

Current repository state:

- forward exact-paper reports live under `../forward/`;
- the current recovered exact ONNX release and shipped `Fw` dataframe are
  forward-only;
- backward exact-paper artifacts will be added after the dedicated rebuild
  from the original dataset.
