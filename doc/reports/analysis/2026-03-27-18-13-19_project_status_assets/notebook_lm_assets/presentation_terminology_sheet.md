# Project Status Presentation Terminology Sheet

- use `StandardML - Codex` as the repository name
- use `transmission error` or `TE` consistently
- use `program-level best` for the global best selected run
- use `family-level best` for the best selected run inside one family
- use `Wave 0`, `Wave 1`, `Wave 2`, and `Wave 3` exactly as roadmap labels
- use `TwinCAT deployment evaluation` for the planned deployment branch
- use `NotebookLM source package` for the repository-owned source bundle
