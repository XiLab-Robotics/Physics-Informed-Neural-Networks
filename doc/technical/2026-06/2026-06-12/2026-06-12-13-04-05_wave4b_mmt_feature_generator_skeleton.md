# Wave 4B MMT Feature Generator Skeleton

## Overview

This technical document defines a non-campaign skeleton pass for `Wave 4B`.
The goal is to prepare an auditable MMT feature-generation interface while a
separate `Track 2H` quantile / probabilistic campaign runs on another
workstation.

The work must keep `Wave 4B` implementation-ready but not campaign-ready. It
must not create real training queues, mutate
`doc/running/active_training_campaign.yaml`, update registries, or consume the
in-flight `Track 2H` results before that campaign is closed out.

The skeleton should reduce future integration risk by making the feature
schema, leakage boundary, configuration template, validator, and dry-run
launcher explicit.

## Technical Approach

The implementation should build on the existing `Wave 4A` diagnostic outputs
and the `Wave 4B` design report. The first pass should expose a conservative
feature-generation package, not a trained model.

The generator should:

- load or reproduce the current `Wave 4A` MMT diagnostic curve boundary;
- define explicit feature groups for global analytical curves, subsystem
  terms, harmonic summaries, and residual-label placeholders;
- classify each feature as inference-safe, train-only calibration material, or
  diagnostic-only;
- emit a small structured feature schema and dry-run companion data under
  `output/validation_checks/wave4b_mmt_feature_generator/`;
- avoid validation-target-derived residuals as inference features;
- avoid any real campaign YAML queue or active-campaign state mutation.

The validator should prove only that the package is internally coherent:
imports work, schema fields are present, generated arrays are finite, and
leakage labels block unsafe fields from inference use.

## Involved Components

Expected source components:

- `doc/reports/analysis/wave4/Wave 4B MMT Feature Generator Design.md`;
- `doc/reports/analysis/wave4/mmt_parameter_inventory/[2026-06-11]/wave4a_mmt_parameter_inventory.md`;
- `doc/reports/analysis/wave4/mmt_equation_diagnostic/[2026-06-11]/wave4a_mmt_equation_diagnostic.md`;
- `scripts/models/wave4_mmt_diagnostic_adapter.py`;
- `scripts/reports/analysis/build_wave4a_mmt_equation_diagnostic_report.py`;
- `doc/reference_summaries/02_MMT_TEModeling_Project_Summary.md`;
- `doc/reference_summaries/06_Programming_Style_Guide.md`.

Expected new or updated implementation components after approval:

- `scripts/features/wave4b_mmt_feature_generator.py`;
- `scripts/campaigns/wave_4/validate_wave4b_mmt_feature_generator_package.py`;
- `scripts/campaigns/wave_4/run_wave4b_mmt_feature_generator_checks.ps1`;
- `config/training/wave4_embryonic_skeleton/wave4b_mmt_feature_generator_template.yaml`;
- `doc/scripts/campaigns/wave_4/wave4b_mmt_feature_generator_checks.md`;
- optional Sphinx API entries if the new user-facing scripts fall within the
  portal scope.

Expected output components after approval:

- `output/validation_checks/wave4b_mmt_feature_generator/`;
- a small schema summary and sample feature table generated by the validator.

No subagent is planned. If a subagent becomes useful later, its proposed name,
task boundary, and approval requirement must be recorded before launch.

## Implementation Steps

1. Create and approve this technical document.
2. Create and approve the paired preliminary non-campaign plan report.
3. Inspect the existing `Wave 4A` diagnostic adapter, parameter inventory,
   diagnostic report generator, and repository style guide.
4. Implement the smallest `Wave 4B` feature generator with explicit feature
   metadata and leakage-use labels.
5. Add a dry-run template config that marks the package as
   `implementation-ready` and `not campaign-ready`.
6. Add a validator that generates a small sample feature payload and rejects
   inference use of train-only or diagnostic-only fields.
7. Add a PowerShell dry-run wrapper and launcher note that clearly state that
   no training campaign is launched.
8. Update documentation indexes and portal entries only where the new commands
   become user-facing.
9. Verify Python compilation, validator execution, dry-run launcher execution,
   scoped Markdown QA, Sphinx `-W` if portal scope changes, and
   `git diff --check`.
10. Stop for explicit user approval before any commit.
