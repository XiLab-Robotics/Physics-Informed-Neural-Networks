# Report Script Documentation

## Overview

The canonical `scripts/reports/` tree is now grouped by responsibility:

- `scripts/reports/closeout/`
- `scripts/reports/presentation/`
- `scripts/reports/pdf/`
- `scripts/reports/analysis/`
- `scripts/reports/track1/`

Use the notes in the matching subfolders when you need the canonical operator
entrypoints.
