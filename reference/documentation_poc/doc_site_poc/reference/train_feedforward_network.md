# FeedForward Training Script

## Overview

This page extends the proof of concept with a richer repository module.

The source module is:

- `scripts/training/train_feedforward_network.py`

This module is a better stress test than the smaller model module because it includes:

- command-line parsing;
- training workflow orchestration;
- terminal-report helpers;
- integration with Lightning, logging, callbacks, and shared infrastructure.

## Generated Reference

::: scripts.training.train_feedforward_network
